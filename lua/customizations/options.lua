-- Editor options changed from the kickstart defaults.
--
-- Relative line numbers: combined with `number` (set by kickstart) this gives a "hybrid"
-- gutter, absolute number on the cursor line and relative distances everywhere else, which
-- makes `5j` / `12k` jumps easy to read off.

vim.o.relativenumber = true
